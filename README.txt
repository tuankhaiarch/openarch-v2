OpenArch independent static build V3
- index.html: standalone landing page
- assets are local in _next/static plus logo files
- form posts to existing Google Apps Script endpoint and stores JSON under Drive backend
